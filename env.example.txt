# Example environment variables (DO NOT put real secrets in this file)
#
# Copy the variables you need into `.env.local` for local development.
# In Netlify, set the same variables in Site settings → Environment variables.

# SMTP config used by /api/contact
# Gmail (recommended) settings:
# - SMTP host: (see Gmail SMTP docs)
# - SMTP port: 587
# - SMTP secure: false
# - SMTP user: your Gmail address
# - SMTP pass: Google App Password (requires 2-Step Verification)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Optional: if you host API separately, set a base URL used by the client components
# NEXT_PUBLIC_API_URL=
