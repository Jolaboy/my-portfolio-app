# Local-only environment variables template
#
# Copy to `.env.local` and fill with your SMTP credentials.
# Never commit `.env.local`.

# SMTP config used by /api/contact
# Gmail (recommended) settings:
# - SMTP host: (see Gmail SMTP docs)
# - SMTP port: 587
# - SMTP secure: false
# - SMTP user: your Gmail address
# - SMTP pass: Google App Password (requires 2-Step Verification)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
