import FullStackPortfolio from '../components/FullStackPortfolio';

/** App Router entry page (single-page portfolio). */
export default function Page() {
  return (
    <main className="min-h-screen">
      <FullStackPortfolio />
    </main>
  );
}
